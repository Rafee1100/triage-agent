# GitHub personal access token (scope: public_repo) — used by GitHubClient for GraphQL
GITHUB_TOKEN=ghp_replace_me

# Groq API key — used by LiteLLM for groq/* model routes (default)
GROQ_API_KEY=gsk_replace_me

# Anthropic API key — only used by LiteLLM when HAIKU_MODEL/SONNET_MODEL switch to anthropic/*
ANTHROPIC_API_KEY=sk-ant-replace_me

# LLM model routing (LiteLLM model strings).
# Default = Groq free tier with Llama 3.3 70B (only Groq model that handles tool
# calling reliably; the 8B-Instant is faster but produces malformed JSON).
# Switch to paid Anthropic later by changing prefix:
#   anthropic/claude-haiku-4-5-20251001
#   anthropic/claude-sonnet-4-5-20251022
HAIKU_MODEL=groq/llama-3.3-70b-versatile
SONNET_MODEL=groq/llama-3.3-70b-versatile

# Telegram bot (optional for local dev; required for morning brief)
TELEGRAM_BOT_TOKEN=replace_me
TELEGRAM_CHAT_ID=replace_me

# CORS — exact origin of the web app (no trailing slash)
# Local dev: http://localhost:3000
# Prod:      https://triagepilot.vercel.app
ALLOWED_ORIGIN=http://localhost:3000

# Default repo to triage if the request omits one (owner/name)
MONITORED_REPO=kubernetes/kubernetes
